import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const Upcoming = () => {
  return (
    <SafeAreaView>
      <Text>{'Upcoming'}</Text>
    </SafeAreaView>
  );
};

export default Upcoming;
